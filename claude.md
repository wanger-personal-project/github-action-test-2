我最近计划学习自动化工作流，目标是打通 本地 push 后可以同步更新到 vercel 中托管的前端页面中，目前想到的步骤和环节有：

1. 找一个基于 next.js 的博客类的框架作为实验目标
2. github 中应该要配置好类似环境变量这样的东西（配置好 vercel 的关键配置）
3. 配置好 github action, 并上传到 github,触发跑 github action 然后部署到 vercel
4. 在 vercel 中查看部署效果
5. 测试在本地 push 后自动触发更新 vercel 页面
6. 配置好个人买的域名
7. 在项目中接入 supebase 做持久化的测试

上面是我的大概计划，请你帮我再梳理一份详细的开发计划，然后写入到 plan.md，有什么不确定的可以交互式和我确认
